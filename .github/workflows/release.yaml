name: Release to NPM
on:
  workflow_dispatch:
    inputs: {}
jobs:
  update:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      # - name: Setup JavaScript Build Environment
      # - name: Publish Package
